import { useSearchParams } from "react-router"
const SearchPage = () => {
  const [searchParams] = useSearchParams()

  // Get data from url and display the results

  return (
    <div>Search</div>
  )
}

export default SearchPage